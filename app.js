console.log('hello world');

let userName = prompt('Hello there, and welcome to my website! We\'re going to play a little game of two truths and a lie (or in this case, three truths and two lies) before we load this page. But first, please tell me your name.');
console.log(userName);

let sample = prompt(`Nice to meet you, ${userName}! Question goes here.`);
console.log(sample);
let normalizedSample = sample.toLowerCase();

if (normalizedSample === 'yes' || normalizedSample === 'y') {
  // console.log('You guessed correct!');
  alert('You guessed correct!')
} else if (normalizedSample === 'no' || normalizedSample === 'n') {
  // console.log('Sorry, you guessed incorrectly.');
  alert('Sorry, you guessed incorrectly.');
} else {
  // console.log('Sorry, I don\'t recognize that response. Next time, please only answer yes or no.');
  alert('Sorry, I don\'t recognize that response. Next time, please only answer yes or no.')
}

let sample = prompt(`Nice to meet you, ${userName}! Question goes here.`);
console.log(sample);
let normalizedSample = sample.toLowerCase();

if (normalizedSample === 'yes' || normalizedSample === 'y') {
  console.log('You guessed correct!');
} else if (normalizedSample === 'no' || normalizedSample === 'n') {
  console.log('Sorry, you guessed incorrectly.');
} else {
  console.log('Sorry, I don\'t recognize that response. Next time, please only answer yes or no.');
}

let sample = prompt(`Nice to meet you, ${userName}! Question goes here.`);
console.log(sample);
let normalizedSample = sample.toLowerCase();

if (normalizedSample === 'yes' || normalizedSample === 'y') {
  console.log('You guessed correct!');
} else if (normalizedSample === 'no' || normalizedSample === 'n') {
  console.log('Sorry, you guessed incorrectly.');
} else {
  console.log('Sorry, I don\'t recognize that response. Next time, please only answer yes or no.');
}

let sample = prompt(`Nice to meet you, ${userName}! Question goes here.`);
console.log(sample);
let normalizedSample = sample.toLowerCase();

if (normalizedSample === 'yes' || normalizedSample === 'y') {
  console.log('You guessed correct!');
} else if (normalizedSample === 'no' || normalizedSample === 'n') {
  console.log('Sorry, you guessed incorrectly.');
} else {
  console.log('Sorry, I don\'t recognize that response. Next time, please only answer yes or no.');
}

let sample = prompt(`Nice to meet you, ${userName}! Question goes here.`);
console.log(sample);
let normalizedSample = sample.toLowerCase();

if (normalizedSample === 'yes' || normalizedSample === 'y') {
  console.log('You guessed correct!');
} else if (normalizedSample === 'no' || normalizedSample === 'n') {
  console.log('Sorry, you guessed incorrectly.');
} else {
  console.log('Sorry, I don\'t recognize that response. Next time, please only answer yes or no.');
}
